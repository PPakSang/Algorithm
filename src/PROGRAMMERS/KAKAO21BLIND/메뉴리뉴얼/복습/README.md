## ☑️복습

String 을 정렬하고 싶다  
  
String.toCharArray() -> Arrays.sort() -> String.valueOf()  
  
String 데이터 자체는 상수로 생각했을 때 char 로 쪼갤 필요가 있다... toCharArray()  
  
Arrays 는 Array Manager 로 역할 수행  ... Arrays.sort(), 다만 주소를 직접 참조해서 따로 저장할 필요 없음