# 🔣 숫자문자열과영단어

문자열 내에서 바꾸고자 하는 부분 문자열을 어떻게 찾고 바꿀 것인지에 대한 문제<br>
String.replace() 를 생각했다면 쉬웠을 문제이지만 Map 에 빠져있어서<br> 
상당히 빙 돌아서 풀었다(Main Class)
☑️replace

replace(찾을 문자열, 바꿀 문자열)

☑️replaceAll

replaceAll(정규식, 바꿀 문자열) : 정규식을 만족하는 문자열 패턴만 바뀐다





