## ☑️풀이

### 배열의 빈칸을 당기는 법

배열 빈칸을 당기는 기준은 방향으로 정해질 수 있는데 가령
  
오른쪽으로 당기고 싶은 경우에는 (여기서 행은 m 열은 n 으로 둘 때)  
0번째 행에서 0~n 열의 값 까지 리스트를 우선만들고, 행을 돌면서 n -> 0 순으로 리스트에 들어간 숫자를 넣는다  

위로 당기고 싶은 경우에는 (여기서 행은 m 열은 n 으로 둘 때)  
0번째 열에서 0~m 행의 값 까지 리스트를 우선만들고, 열을 돌면서 m -> 0 순으로 리스트에 들어간 숫자를 넣는다  
  
  
